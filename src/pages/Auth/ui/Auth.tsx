import { AuthWrapper } from "@/pages/Auth/ui/styled";
import { AuthForm } from "@/widgets/ui/AuthForm";

export const Auth = () => {
  return (
    <AuthWrapper>
      <AuthForm />
    </AuthWrapper>
  );
};
