import { RegistrationWrapper } from "@/pages/Registration/ui/styled";
import { RegistrationForm } from "@/widgets/ui/RegistrationForm";

export const Registration = () => {
  return (
    <RegistrationWrapper>
      <RegistrationForm />
    </RegistrationWrapper>
  );
};
